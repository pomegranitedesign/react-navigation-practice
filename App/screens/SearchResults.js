import React from 'react'
import { View, Text, StyleSheet } from 'react-native'

const SearchResults = (props) => {
	return (
		<View>
			<Text>This is Your search results</Text>
		</View>
	)
}

const styles = StyleSheet.create({})

export default SearchResults
